extension:
  name: duck_net
  description: Comprehensive HTTP/HTTPS network client for DuckDB
  version: 0.1.0
  language: rust
  excluded_platforms: "wasm_mvp;wasm_eh;wasm_threads"
  requires_toolchains: "rust"
  build:
    path: "."
